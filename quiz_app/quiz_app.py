import randomclass QuizApp:    def __init__(self):        self.questions = {            "history": [                {"question": "Who was the first president of the USA?", "options": ["George Washington", "Abraham Lincoln", "Thomas Jefferson"], "answer": "George Washington"},                {"question": "When did World War II end?", "options": ["1942", "1945", "1950"], "answer": "1945"}            ],            "science": [                {"question": "What is the chemical symbol for water?", "options": ["O2", "H2O", "CO2"], "answer": "H2O"},                {"question": "What is the force that pulls objects towards the center of the Earth?", "options": ["Magnetism", "Friction", "Gravity"], "answer": "Gravity"}            ]        }        self.score = 0    def display_question(self, q_data):        print(f"\nQuestion: {q_data['question']}")        for i, option in enumerate(q_data['options']):            print(f"{i+1}. {option}")    def get_user_answer(self, num_options):        while True:            try:                choice = int(input(f"Enter your choice (1-{num_options}): "))                if 1 <= choice <= num_options:                    return choice                else:                    print("Invalid choice. Please enter a number within the range.")            except ValueError:                print("Invalid input. Please enter a number.")    def run_quiz(self, category):        if category not in self.questions:            print("Invalid category.")            return        questions_in_category = self.questions[category]        random.shuffle(questions_in_category)        for q_data in questions_in_category:            self.display_question(q_data)            user_choice_index = self.get_user_answer(len(q_data['options'])) - 1                        if q_data['options'][user_choice_index] == q_data['answer']:                print("Correct!")                self.score += 1            else:                print(f"Wrong! The correct answer was: {q_data['answer']}")        print(f"\nQuiz finished! Your score: {self.score}/{len(questions_in_category)}")if __name__ == "__main__":    quiz = QuizApp()    print("Welcome to the Quiz App!")    print("Available categories: history, science")        while True:        chosen_category = input("Enter a category to start the quiz (or 'exit' to quit): ").lower()        if chosen_category == 'exit':            print("Goodbye!")            break        elif chosen_category in quiz.questions:            quiz.run_quiz(chosen_category)            quiz.score = 0 # Reset score for next quiz        else:            print("Category not found. Please choose from available categories.")